{
  "name": "fees-amount-displayer",
  "license": "MIT",
  "description": "Simple module to calculate and display fees from JSON file input",
  "type": "module",
  "main": "app.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/ravlasevic/fees-amount-displayer.git"
  },
  "scripts": {
    "test": "mocha --experimental-modules --es-module-specifier-resolution=node --recursive --timeout 5000",
    "calculate": "node --experimental-modules --es-module-specifier-resolution=node app.js"
  },
  "dependencies": {
    "chai": "^4.3.4",
    "dirty-json": "^0.9.2",
    "eslint": "^7.30.0",
    "eslint-config-airbnb": "^18.2.1",
    "eslint-plugin-import": "^2.22.1",
    "mocha": "^9.0.0"
  }
}
